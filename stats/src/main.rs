use std::io::stdin;
fn main() {
    struct value {
        first: i32,
        last: i32,
        mid: i32,
        frequency: i32,
    }
    let mut iternation: String::new();
    stdin().read_line(&mut iternation).unwrap();
    println!("iternation {iternation} ");
}
fn mean_deviation() {}
